<div class='span4 offset4'>
    <%= form_tag login_path, { :class => "well" } do %>
      <div class='text-center'>
          <%= image_tag('logo.png') %>
      </div>
      <%= bootstrap_flash %>

      <div class="control-group">
        <%= label_tag :username, 'Usuario', :class=>'control-label' %>
        <div class="controls">
          <%= text_field_tag :username, nil, :class=>'span12' %>
        </div>
      </div>

      <div class="control-group">
        <%= label_tag :password, 'Contraseña', :class=>"control-label" %>
        <div class="controls">
          <%= password_field_tag :password, nil, :class => 'span12' %>
        </div>
      </div>

      <div class="control-group">
        <div class="controls">
          <%= submit_tag 'Ingresar', :class =>'btn btn-primary' %>
        </div>
      </div>
    <% end %>
</div>