<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : 'CUALimpieza' %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'application', :media => 'all' %>

    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="#">CUALimpieza</a>
            <!-- TODO: Hacer el menu no estatico (https://github.com/seyhunak/twitter-bootstrap-rails) -->
            <ul class="nav">
              <% class_type = (@current_module == 'limpieza') ? 'active' : 'inactive' %>
              <li class="<%= class_type %>"><%= link_to 'Limpieza', limpieza_general_path  %></li>
              <% class_type = (@current_module == 'personal') ? 'active' : 'inactive' %>
              <li class="<%= class_type %>"><%= link_to 'Personal', personal_employees_list_path  %></li>
              <% class_type = (@current_module == 'inventario') ? 'active' : 'inactive' %>
              <li><%= link_to 'Inventario', '#'  %></li>
            </ul>

            <ul class="nav pull-right">
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <i class="icon-user"></i>
                  <strong><%=@current_user.complete_name%></strong>
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <!--<li><%= link_to 'Mi cuenta', '#' %></li>-->
                  <li class="divider"></li>
                  <li><%= link_to 'Cerrar sesión', '/home/logout' %></li>
                </ul>
              </li>
            </ul>
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span3">
          <% if @current_module == 'limpieza' %>
            <%= render 'shared/cleanup_sidebar' %>
          <% elsif @current_module == 'personal' %>
            <%= render 'shared/personal_sidebar'%>
          <% end %>
        </div><!--/span-->
        <div class="span9">
          <%= bootstrap_flash %>

          <div id="realtime-notification-container"></div>
          <div id="realtime-notification-template" class="alert alert-info realtime-notification hide">
            <button type="button" class="close" data-dismiss="alert">×</button>
          </div>

          <div id='yield-container'>
            <%= yield %>
          </div>
        </div>
        <%= render 'shared/modal_popup'%>
      </div><!--/row-->

      <footer>
        <!--<p>&copy; IIC3143 2013</p>-->
      </footer>

    </div> <!-- /container -->
  </body>
</html>
