<%= form_tag action_url, :method => 'get', :id => 'rooms-search' do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], :class=>'input-large'  %>
      <%= submit_tag search_label, :class=>'btn' %>
    </div>
<% end %>